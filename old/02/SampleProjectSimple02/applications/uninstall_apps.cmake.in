# Uninstall script for @EXE_TARGET_NAME@
# Generated by CMake

if (EXISTS "@APP_INSTALL_DIR@")
    set(executable_path "@APP_INSTALL_DIR@/@EXE_TARGET_NAME@.exe")
    if (EXISTS "${executable_path}")
        file(REMOVE "${executable_path}")
        message(STATUS "Removed executable: @EXE_TARGET_NAME@.exe")
    else()
        message(STATUS "Executable not found: @EXE_TARGET_NAME@.exe")
    endif()
else()
    message(FATAL_ERROR "Cannot uninstall. APP_INSTALL_DIR is not defined.")
endif()
